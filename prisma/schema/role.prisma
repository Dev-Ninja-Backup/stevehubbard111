model Role {
  id    Int    @id @default(autoincrement())
  name  Roles @unique
  users User[]
  createdAt               DateTime @default(now())
  updatedAt               DateTime @updatedAt

  @@map("roles")
}
